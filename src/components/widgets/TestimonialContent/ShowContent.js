import React from 'react'
// Definition of ShowContent component to render children
const ShowContent = ({ children }) => {
    return (
       <div>
           {children}
       </div>
    )
}

export default ShowContent